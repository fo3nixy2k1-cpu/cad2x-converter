# 2026-03-24 日记

## Gateway 健康检查
- 13:59 OK — Chrome=8, Node=3, GwMem=848MB
- 14:05 OK — TIME_WAIT=2, GwMem=807MB
- 14:45 OK — GwMem=933MB
- 16:26 OK — GwMem=659MB, GwPID=6572

## 今日工作记录

### 健康检查修复
- 重复 feishu 插件问题：stock feishu 和本地扩展冲突，本地 entry 已禁用
- wecom 插件报错：已禁用
- 版本已更新到 2026.3.23-2

### 203 服务器问题
- ESXi 显示 8G 内存满，实际是 OOM killer 杀死进程
- 原因：browser 控制超时 → 卡死 Gateway → 系统 kill
- 解决方案：203 上禁用 browser 工具 (`browser.enabled: false`)
- 重启后恢复

### 通信系统重建（Python 版）【重要！已成功】
将 relay 系统从 JS 改为 Python（更稳定），端到端测试成功。

**架构：**
```
本机星火(195) ←→ Relay Hub(18080) ←→ Sidecar(195:18081) ← 本机 OpenClaw
                ↓
          Sidecar(203:18081) ←→ 小明(203 OpenClaw)
```

**文件位置：`C:\Users\y2k1\relay_py\`**
- `relay_hub.py` — Hub 主程序，监听 18080，路由消息
- `relay_sidecar_195.py` — 本机 sidecar，监听 18081，通知 OpenClaw
- `relay_sidecar_203.py` — 203 sidecar，监听 18081，转发消息给 203 OpenClaw
- `test_relay.py` — 测试脚本，发消息给 203
- `test_webhook.py` — 测试 Hub webhook
- `test_claw_direct.py` — 测试 /v1/responses 接口
- `upload_203.py` — 上传文件到 203
- `redeploy.py` — 重新部署 203 sidecar
- `get_token.py` — 读取 token

**Token：**
- 本机 Gateway token: `clawx-b669c76c4ec27a7b8d2892303063873a`
- 203 Gateway token: `sk-b669c76c4ec27a7b8d2892303063873b`

**关键问题排查：**
1. Hub 调用 claw 用错端口 18789 → 实际是 28789
2. 203 连 Hub 超时 → Windows 防火墙没开放 18080 → 已添加入站规则
3. Hub 同步调用 claw 阻塞响应 → 改成异步 thread + 结果写文件

**使用方式：**
```python
# 发消息给 203 小明
body = {'sender': 'xinghuo', 'target': 'qiming', 'topic': 'xxx', 'content': '消息内容'}
# POST to http://127.0.0.1:18080/relay
# 小明回复会写入 C:\Users\y2k1\.openclaw\relay_results\result_{topic}.txt
```

**问题：**
- Hub/Sidecar 未做成开机自启动，每次 Gateway 重启后需手动启动
- 日志没有持久化（Python stdout 被缓冲）

## 201 火火部署（2026-03-25 凌晨）

**关键发现：**
- 201 之前是 openclaw 2026.3.11（旧版），升级到 2026.3.23-2 后 /v1/responses 才可用
- 201 Gateway token: `af83d54dae9fd044ced5005f1cbdfb00b7636317c3143a73`
- 201 claw 绑定 127.0.0.1:18789（loopback），sidecar 用 127.0.0.1 调用
- 201 sidecar 配置：CLAW_URL = `http://127.0.0.1:18789/v1/responses`，CLAW_TOKEN = `af83d54dae9fd044ced5005f1cbdfb00b7636317c3143a73`

**Hub agents 配置：**
```python
agents = {
    'xinghuo': {'sidecar': 'http://127.0.0.1:18081/webhook/agent', 'claw_url': claw_url, 'token': hub_token},
    'huohuo':  {'sidecar': 'http://192.168.10.201:18081/webhook/agent', 'claw_url': 'http://192.168.10.201:18789/v1/responses', 'token': 'af83d54dae9fd044ced5005f1cbdfb00b7636317c3143a73'},
    'qiming':  {'sidecar': 'http://192.168.10.203:18081/webhook/agent', 'claw_url': 'http://192.168.10.203:18789/v1/responses', 'token': 'sk-b669c76c4ec27a7b8d2892303063873b'},
}
```

**自启动问题（已解决）：**
- Hub/Sidecar 用 Startup 文件夹方式自启动
- bat 文件已复制到 `$env:APPDATA\Microsoft\Windows\Start Menu\Programs\Startup\`
- `start_hub.bat` 和 `start_sidecar.bat` 放在 `C:\Users\y2k1\relay_py\`

**最终状态（2026-03-25凌晨）：**
- Hub (18080) ✅ 运行中
- 本机 Sidecar (18081) ✅ 运行中
- 201 火火 Sidecar ✅ 运行中，稳定回复
- 203 小明 Sidecar ✅ 运行中
- Startup 自启动 ✅ 已配置

### 203 小明今日工作
- 从记忆文件读到的：学习浏览器自动化（snapshot/act）
- 完成了：type、click、select、checkbox、wait networkidle、多标签页、dialog、upload、iframe
- 遇到问题：snap chromium 安装、DISPLAY 缺失（用 headless）、w3schools 慢（加 wait）、upload 路径限制

### SSH 工具更新
- server-comm skill 已改用 Python + paramiko（之前 JS 不稳定）
- 脚本：`C:\Users\y2k1\.openclaw\workspace\skills\server-comm\ssh_func.py`
- 旧零散 JS 脚本已清理
